// make sure it's the same order as the dropdown in the loadout

public enum Melees
{
    Assassin = 0,
    Bruiser = 1,
    Tank = 2
}

public enum Rangeds
{
    Hunter = 0,
    Scout = 1,
    Trapper = 2
}

public enum Mages
{
    Wizard = 0,
    Summoner = 1,
    Healer = 2
}